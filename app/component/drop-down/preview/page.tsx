import { DropDown } from "@/components";
import { Metadata } from "next";
const Page = () => {
  return <DropDown />;
};

export default Page;

export const metadata: Metadata = {
  title: "Drop down preview",
};
